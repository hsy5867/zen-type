"""zen-type — AI voice input tool."""

__version__ = "2.0.2"
__release_notes__ = (
    "2.0.2  — overlay pill renders at native pixel size from the first frame "
    "on high-DPI Windows displays (declares per-monitor DPI awareness before "
    "Tk init), eliminating the initial bitmap-stretched 'large icon' state"
)
