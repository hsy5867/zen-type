"""zen-type — AI voice input tool."""

__version__ = "2.0.6"
__release_notes__ = (
    "2.0.6  — exe file icon is now the brand mic-on-disc (Latte accent + "
    "white SVG mic glyph), bundled as a multi-resolution .ico (16/24/32/"
    "48/64/128/256) and applied via PyInstaller --icon. Replaces the "
    "default PyInstaller bootloader icon shown in File Explorer / taskbar."
)
